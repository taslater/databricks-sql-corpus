# SHOW VOLUMES. The statement with an optional schema and pattern parses;
# this page pins it.
statement: SHOW VOLUMES
doc: https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-syntax-aux-show-volumes
checked: 2026-09-20
syntax: |
  SHOW VOLUMES [ { FROM | IN } schema_name ] [ [ LIKE ] regex_pattern } ]
cases:
  - id: show-volumes.basic
    verdict: must-parse
    anchor: "#syntax"
    sql: |
      SHOW VOLUMES
  - id: show-volumes.in-schema
    verdict: must-parse
    anchor: "#examples"
    sql: |
      SHOW VOLUMES IN s
